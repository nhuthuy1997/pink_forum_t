$('.<%= Settings.target.classname[@action] %>-list').replaceWith(
  "<%= j render partial: 'admins/users/users_list', locals: { users: eval(Settings.target.variable[@action]),
    add_morderator: true, source: @category, action: @action } %>"
);

minus_morderators();
add_morderators();
ban_users();
unban_users();
